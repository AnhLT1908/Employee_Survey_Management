@model HRTestWeb.Areas.Admin.Controllers.TestEditVM
@{
    ViewData["Title"] = "Sửa bài test";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <h3 class="mb-3">Sửa bài test</h3>

    <form asp-area="Admin" asp-controller="Tests" asp-action="Edit" method="post" class="card shadow-sm">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />

        <div class="card-body row g-3">
            <div class="col-12">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger small"></span>
            </div>

            <div class="col-12">
                <label asp-for="Description" class="form-label"></label>
                <textarea asp-for="Description" class="form-control" rows="2"></textarea>
            </div>

            <div class="col-md-4">
                <label asp-for="DurationMinutes" class="form-label"></label>
                <input asp-for="DurationMinutes" class="form-control" />
                <span asp-validation-for="DurationMinutes" class="text-danger small"></span>
            </div>

            <div class="col-md-4">
                <label asp-for="PassScore" class="form-label"></label>
                <input asp-for="PassScore" class="form-control" />
                <span asp-validation-for="PassScore" class="text-danger small"></span>
            </div>

            <div class="col-md-4">
                <label class="form-label">Số câu hỏi</label>
                <div class="input-group">
                    <input asp-for="QuestionCount" class="form-control" />
                    <span class="input-group-text">hiện có: @Model.CurrentQuestionCount</span>
                </div>
                <div class="form-text">Chỉ áp dụng khi bật “Xáo trộn lại câu hỏi”.</div>
                <span asp-validation-for="QuestionCount" class="text-danger small"></span>
            </div>

            <div class="col-12">
                <div class="form-check">
                    <input class="form-check-input" asp-for="RegenerateQuestions" />
                    <label class="form-check-label" asp-for="RegenerateQuestions"></label>
                </div>
                <div class="form-text">
                    Khi bật, hệ thống sẽ chọn ngẫu nhiên lại câu hỏi theo bộ lọc bên dưới và thay thế danh sách hiện tại.
                </div>
            </div>

            <div class="col-md-6">
                <label asp-for="BankId" class="form-label"></label>
                <select asp-for="BankId" asp-items="Model.Banks" class="form-select"></select>
                <span asp-validation-for="BankId" class="text-danger small"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="Difficulty" class="form-label"></label>
                <select asp-for="Difficulty" class="form-select">
                    <option value="">-- Tất cả độ khó --</option>
                    <option value="0">Intern</option>
                    <option value="1">Fresher</option>
                    <option value="2">Junior</option>
                    <option value="3">Middle</option>
                    <option value="4">Senior</option>
                    <option value="5">Lead</option>
                    <option value="6">Manager</option>
                </select>
            </div>

            <div class="col-md-6">
                <label asp-for="Type" class="form-label"></label>
                <select asp-for="Type" class="form-select">
                    <option value="">-- Tất cả loại --</option>
                    <option value="0">Trắc nghiệm (MCQ)</option>
                    <option value="1">Tự luận</option>
                    <option value="2">Đúng / Sai</option>
                </select>
            </div>

            <div class="col-md-6">
                <label asp-for="LevelId" class="form-label"></label>
                <select asp-for="LevelId" asp-items="Model.Levels" class="form-select">
                    <option value="">-- Không ràng buộc level --</option>
                </select>
            </div>

            <!-- Thời gian hiệu lực cho Assignment -->
            <div class="col-md-6">
                <label asp-for="StartAt" class="form-label"></label>
                <input asp-for="StartAt" class="form-control" type="datetime-local" />
                <span class="form-text text-muted">Bỏ trống nếu không giới hạn.</span>
                <span asp-validation-for="StartAt" class="text-danger small"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="EndAt" class="form-label"></label>
                <input asp-for="EndAt" class="form-control" type="datetime-local" />
                <span class="form-text text-muted">Bỏ trống nếu không giới hạn.</span>
                <span asp-validation-for="EndAt" class="text-danger small"></span>
            </div>

            <div class="col-md-6">
                <label class="form-label">Áp dụng cho Roles (không gồm Admin)</label>
                <select asp-for="SelectedRoles" asp-items="Model.Roles" class="form-select" multiple size="6"></select>
                <div class="form-text">Giữ Ctrl/Shift để chọn nhiều role.</div>
            </div>
        </div>

        <div class="card-footer d-flex justify-content-between">
            <a class="btn btn-link" asp-area="Admin" asp-controller="Tests" asp-action="Index">← Quay lại</a>
            <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
